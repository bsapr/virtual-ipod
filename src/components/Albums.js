import React from 'react';

class Albums extends React.Component {
  render() {
    return (
      <div id="albums" className="noContent">
        Albums
      </div>
    );
  }
}

export default Albums;
